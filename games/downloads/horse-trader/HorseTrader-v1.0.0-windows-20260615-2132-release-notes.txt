HorseTrader v1.0.0 Windows build 20260615-2132

Public desktop multiplayer join-flow fix build.

Changes:
- Fixed the desktop join flow so the joining player enters host address and room code first.
- Added an in-app seat chooser after the room is found, so color/player seat is chosen from the actual room.
- Removed the pre-join color picker from the desktop splash screen.
- Fixed the manual server start path so it listens on the LAN interface instead of only 127.0.0.1.
- Kept the launcher downgrade guard so newer local builds are not replaced by older public builds.
- Kept crash logging for host/join failures in HorseTrader_crash_log.txt.
- Kept latest.zip matched to this build.

What to check in a real multiplayer join:
- Host starts a room from Play HorseTrader.exe.
- Joining player enters the host address and room code.
- Joining player should then see a seat chooser with the room's available human player seats.
- Joining player picks a seat and enters the game.

Tested:
- Python compile check.
- Server smoke test.
- Desktop multiplayer smoke test.
- AI scenario tests.
- Build-script test gate before packaging.
- Folder-wrapped ZIP layout.
- Confirmed updater skips downgrading this build to the older public build.
