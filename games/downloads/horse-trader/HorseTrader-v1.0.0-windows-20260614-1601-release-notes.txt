HorseTrader v1.0.0 Windows build 20260614-1601

Public desktop multiplayer test build.

Changes:
- Fixed multiplayer polling so a newer server state always reaches the client, even if that client has a stale local choice open.
- This targets the case where one player sees the victory screen but the other player does not.
- Added a desktop multiplayer smoke test that finishes a game on one client and verifies the other client receives the final score screen.
- Kept Play HorseTrader.exe as the clear player-facing starter.
- Kept latest.zip matched to this build.

Notes:
- Undo remains local-only in multiplayer rooms for now. Network-safe undo needs a separate design because it would have to roll back the shared server state for all players.

Tested:
- Python compile check.
- Server smoke test.
- Desktop multiplayer smoke test, including movement action submission, wrong-seat blocking, and final-score sync after stale local choice state.
- AI scenario tests.
- Build-script test gate before packaging.
- Folder-wrapped ZIP layout.
- Confirmed Play HorseTrader.exe is inside the ZIP.
