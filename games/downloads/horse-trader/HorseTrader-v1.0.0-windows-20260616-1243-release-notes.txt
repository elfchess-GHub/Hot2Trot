HorseTrader v1.0.0 Windows build 20260616-1243

Public desktop multiplayer final-score popup fix build.

Changes:
- Restored the final scoring window as the endgame popup instead of showing the scoreboard in the right player panel.
- Fixed the multiplayer finisher path so the player who buys the Global Writ also gets the final-score popup.
- Kept the final-score popup syncing to the other multiplayer client.
- Added a desktop multiplayer smoke check that requires both finisher and spectator clients to receive the final-score popup.
- Kept latest.zip matched to this build.

Tested:
- Python compile check.
- Server smoke test.
- Desktop multiplayer smoke test, including final-score popup on both clients.
- AI scenario tests.
- Build-script test gate before packaging.
- Clean packaged build folder check.
